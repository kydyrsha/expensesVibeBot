[project]
name = "expenses-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3,<4",
    "APScheduler>=3.10,<4",
    "supabase>=2,<3",
    "fastapi>=0.117",
]

[tool.vercel]
entrypoint = "app:app"
